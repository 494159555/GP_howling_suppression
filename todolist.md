# 毕业论文 Word 手动修改清单

> 以下内容基于 `thesis_md/` 源文件和 pandoc 转换结果分析，需在 `thesis_word/thesis_pandoc.docx` 中手动修改。

---

## D. 需插入图片的位置

### D1. 图2-1 声反馈正反馈过程示意图（必须）
- **位置**: 第2章 2.1.2节末尾，"> **[此处插入图2-1...]**" 引用块处
- **来源文件**: `02_chapter.md` 第 51 行
- **现状**: md源文件中已替换为文本占位符（含信号流程文字描述），pandoc 转换后为引用块文本
- **操作**: 删除 Word 中的占位文本，手动插入信号流程框图（用 PPT/Visio/draw.io 绘制：麦克风→放大器(G)→扬声器→声学路径h(t)→反馈信号d(t)→回到麦克风，形成闭环）

### D2. 图4-X(a) 干净语音频谱图（必须）
- **位置**: 第4章 4.5.1节 "干净语音的频谱呈现典型语音特征..." 段落前
- **来源文件**: `04_chapter.md` 第 152 行
- **现状**: 4.5.1节纯文字描述了频谱特征，无实际图片
- **操作**: 插入干净语音的时频频谱图，标注"图4-X(a) 干净语音频谱"

### D3. 图4-X(b) 带啸叫语音频谱图（必须）
- **位置**: 第4章 4.5.1节 "带啸叫语音的频谱在3500Hz处出现..." 段落前
- **来源文件**: `04_chapter.md` 第 152 行
- **现状**: 纯文字描述
- **操作**: 插入带啸叫语音的时频频谱图，标注"图4-X(b) 带啸叫语音频谱"，3500Hz处应有明显亮线

### D4. 图4-X(c)-(g) 各方法处理后频谱图（必须）
- **位置**: 第4章 4.5.1节 "移频法处理后..." 至 "AudioUNet5Optimized的处理效果..." 段落前
- **来源文件**: `04_chapter.md` 第 154-156 行
- **现状**: 纯文字描述，无实际图片
- **操作**: 插入各方法（移频法、自适应反馈消除法、AudioUNet3、AudioUNet5Attention、AudioUNet5Optimized）处理后频谱图，建议做成子图拼接（共用一个图号）
- **建议**: D2-D4 合并为一张大图，如"图4-7 典型样本频谱对比：（a）干净语音（b）带啸叫语音（c）移频法（d）自适应反馈消除法（e）AudioUNet3（f）AudioUNet5Attention（g）AudioUNet5Optimized"

### D5. 图4-X 模型性能演进可视化（建议）
- **位置**: 第4章 4.5.2节
- **来源文件**: `04_chapter.md` 第 158-160 行
- **现状**: 纯文字描述性能数据，无柱状图/折线图
- **操作**: （可选）插入各模型 SI-SDR/PESQ/STOI 对比柱状图

### D6. 图4-X 训练过程可视化（建议）
- **位置**: 第4章 4.5.3节
- **来源文件**: `04_chapter.md` 第 162-164 行
- **现状**: 纯文字描述训练曲线走势，无实际图片
- **操作**: （可选）插入训练损失/SI-SDR/PESQ 随 epoch 变化的曲线图

---

## E. 需检查公式的位置

### ~~E1. 公式(2.1)——`\tag` 未转换~~ ✅ 已修复
- **修复内容**: md源文件中所有 21 个 `\tag{X.Y}` 已替换为 `\qquad (X.Y)`，pandoc 可正常转换为 Word OMML 公式
- **Word 操作**: 检查公式编号 (2.1)-(2.21) 是否显示在公式右侧，编号作为公式内容的一部分（用 `\qquad` 间隔）

### ~~E2. 公式(2.9)——`\tag` 未转换~~ ✅ 已修复
- **修复内容**: 同上，`\tag{2.9}` 已替换
- **Word 操作**: 同上

### ~~E3. 其他 19 个带 `\tag` 的公式~~ ✅ 已修复
- **修复内容**: 所有 `\tag{X.Y}` 统一替换为 `\qquad (X.Y)`，编号嵌入公式内部
- **Word 操作**: 逐一检查公式 (2.1)-(2.21) 的编号是否正确显示在公式右侧。如需更专业的排版，可在 Word 中选中公式编号部分，剪切后作为独立的右对齐文本放置

---

## F. 其他 Word 排版检查项

### F1. 表格标题位置
- **涉及表格**: 表3-1(数据集规模)、表4-1至表4-6
- **现状**: Markdown 中表格标题在表格上方，pandoc 转换后标题可能变为普通段落
- **操作**: 确认每个表格的标题（如"表3-1 数据集规模统计"）显示在表格上方，若不对需调整

### F2. 表4-3 中的 ✓ ✗ 符号
- **位置**: 表4-3（消融实验结果）
- **现状**: pandoc 已正确转换 ✓ ✗ 符号
- **操作**: 在 Word 中目视确认符号显示正常，字体无乱码

### F3. 参考文献格式
- **位置**: 第7章 参考文献
- **现状**: pandoc 已转换，30 条文献均存在
- **操作**: 检查格式是否符合学校要求（[J]期刊/[C]会议/[D]学位论文/[S]标准 等类型标注是否保留）

### F4. 中英文混排字体
- **操作**: 全文检查中文字体（通常为宋体/Times New Roman）和英文字体是否正确

### F5. 章节标题格式
- **现状**: pandoc 已使用 `--reference-doc` 模板，标题层级为 Heading 1/2/3
- **操作**: 检查章标题（第X章）是否居中、节标题（X.X）是否左对齐、字号是否符合模板要求

### F6. 目录
- **现状**: pandoc `--toc` 已生成目录
- **操作**: 确认目录页的格式、缩进和页码是否正确，更新目录（右键→更新域）

---

## 已完成事项（保留记录）

<details>
<summary>点击展开已完成事项</summary>

### A. 内容补充与精简 ✅
- A1-A3: 原始todolist中A类未完成项
- 论文精简: 共精简约2,700字（从20,313→17,610中文字），消除27处章节间重复

### B. 格式修复 ✅
- B1: 所有md文件已添加 `#`/`##`/`###` Markdown标题语法（90个标题）
- B2: pandoc转换命令已确定，使用 `--reference-doc` 和 `--toc`

### C. 转换前检查 ✅
- C1: pandoc 3.9.0.2 已安装在 GraduationProject 环境
- C2: 转换后检查已完成，主要问题已定位（见 D/E/F 清单）

### G. 公式修复 ✅
- G1: 02_chapter.md 中 21 个 `\tag{X.Y}` 全部替换为 `\qquad (X.Y)`，消除 pandoc "Could not convert TeX math" 错误
- G2: `\boxed{...}` LaTeX 流程图替换为引用块文本占位符，需在 Word 中替换为实际图片（见 D1）

</details>
