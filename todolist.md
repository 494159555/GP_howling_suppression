# 毕业论文待办事项

## 数据问题（优先级：高）

- [x] **检查传统方法实现代码** — 已修复：
  - GainSuppression：向量化啸叫检测，推理时间从31s降至38ms
  - AdaptiveFeedback：改为逐频率bin NLMS + 增益衰减方式，修复输出趋近零的问题
  - ⚠️ 需要重新运行 `python scripts/exp2_evaluate_models.py` 获取新的评估数据
- [x] **补充未处理信号基线数据** — 已在 `exp2_evaluate_models.py` 中添加 `evaluate_baseline()` 函数，计算 clean vs noisy 的 SI-SDR、PESQ、STOI
  - ⚠️ 需要重新运行 `python scripts/exp2_evaluate_models.py` 获取基线指标
- [x] **为传统方法补充SI-SDR和PESQ指标** — 已在 `evaluate_traditional_methods` 的 `method_metrics` 中添加 `si_sdr_db` 和 `pesq_score`，对比表格也已更新
  - ⚠️ 需要重新运行 `python scripts/exp2_evaluate_models.py` 获取完整指标

## 实验补充（优先级：中）

- [ ] **考虑重新运行消融实验** — 当前消融结果显示注意力和空洞卷积的边际贡献为负（去掉后SI-SDR反升），与论文叙述存在矛盾。可能的改进方向：
  - 检查消融实验的训练是否充分收敛（当前50个epoch）
  - 尝试不同随机种子多次运行取平均
  - 调整组件的配置参数（如注意力门的通道缩减比、空洞卷积的空洞率）
- [ ] **数据增强策略效果有限** — 各增强策略的SI-SDR差异仅0.07dB。可尝试：
  - 增大掩蔽比例和噪声强度
  - 添加更多增强手段（如频谱拉伸、随机频移等）
  - 增加训练epoch以让模型更好地利用增强数据
- [ ] **补充OneCycleLR + FullAugmentation的组合实验** — 当前最优LR策略（OneCycleLR, SI-SDR 11.66）和最优增强（FullAugmentation, SI-SDR 11.62）分别来自不同实验，未测试两者的组合效果

## 论文文本改进（优先级：中）

- [ ] **调整第3章模型架构描述中的参数量** — 论文中AudioUNet3参数量写为1.2M，实际为0.05M；AudioUNet5写为4.5M，实际为0.88M等。需要根据实际模型结构更新第3章的网络架构描述
- [ ] **统一"收敛轮数"描述** — 原文损失函数和学习率策略对比中引用了"收敛轮数"指标，但实验数据中未明确记录该值（所有实验以固定epoch数运行）。需要：
  - 从训练日志中提取验证损失首次达到最优附近（如1%范围内）的epoch
  - 或统一删除该指标的对比
- [ ] **审查5.2节中的参数量引用** — 原文写"约620万个参数"，实际为313万（3.13M），需要更新

## 可选优化（优先级：低）

- [ ] **增加模型可重复性** — 固定随机种子，多次运行取均值和标准差，增强结果可信度
- [ ] **补充统计显著性检验** — 各模型/策略之间的性能差异较小（如OneCycleLR vs WarmupCosine仅差0.01dB），可通过显著性检验判断差异是否具有统计意义
- [ ] **添加置信区间** — 在结果表格中为每个指标添加标准差或置信区间
